This folder is for demonstration purposes, and is NOT part of the submission. It is used to make outputs/ to show automatically, once. In fact, scheduler.py has been running for an entire real calendar month, Without waiting for real time to elapse before the due date of submission.
 WHY JUNE 2026, NOT JULY
 "Today" in this session is 2026-07-09 , week 2 of July. July's later Fridays have yet to occur (17, 24, 31) and a demo created on July would Reports have been provided for dates that have yet to occur. June 2026 is a year ago from today, and all 4 of its weekly checkpoints (Jun 5, 12, 19, 26 , June has 4 Fridays, one fewer than July's 5, representing both cases the actual production of the pipeline will be in July. The following dates are dates that actually may have actual reports (on the month): by now.
WHAT IT CONTAINS 
One pair of 4 weekly reports (JSON + PDF) per project for each actual Friday. The monthly and client decks were also built, and will be handed out in June 2026. from all those 4 checkpoints per project – in the same exact. The code in the rags_engine.py / data_loader.py / pdf_report.py / presentation.py files is specific to the program. The actual pipeline application requirements. There is no logic used here which is different from the real Only the evaluation dates are simulated; there is no submission. 
WHAT IS REAL ACROSS THESE 4 CHECKPOINTS 
All date driven signals are actually recalculated and actual for each. The in-week Red rule is the essential condition that must be checked on the airtime in the context of the overdue-ness (checkpoint date) condition. The overdue count, milestone lateness, blocker age, and working-day data are used for this purpose. Baseline vs. Actual comparison. The per-task detail (all_task_status) The genuine week over week is based on the count of rows that were classified as Red.genuine week over week , e.g. Titan's Red-classified row count is based on the number of rows classified as Red by this service. The 4 checkpoints are marked with the numbers 1, 1, 3, and 4, with more tasks crossing the 4 checkpoints than others. their real overdue thresholds.
 However, the portfolio-level composite score for the first time appears flatter across. these special 4 weeks (Titan at 65, UniSan at ~90) , that's a No bug, real and honest consequence of thresholded scoring: underlying signals (<5%/5%-20%/>20%-overdue bands) are banded, so that does not always mean that more overdue assignments move the band. Composes until a threshold is reached. The task-level detail Even if the monthly charts suggest otherwise, the real week-by-week movement is clearly seen above. composite band is yet to flip. 
A one honest limitation is the truth that there is no such thing as what is not real.
% Complete and Status are only a single "snapshot" of the current state. The .xlsx files that are the sources, it doesn't matter what they are, as there is no history. were on any past date. Thus, at each of the 4 points listed above, % Complete, Status are the same real, frozen. TODAY'S values as are the Not historical values for that date but values from the source file. This is the exact same limitation as documented in the README.md "Known" section. No less is glossed-over here either in the "limitations" section. 
The following is an explanation of why this is ok for use as a demo. 
The actual report for the week is located in outputs/ and IS ONE report of each week, That's really all the sample data will support when submitted" day, this is a real folder, not simulated. This outputs_demo/ folder exists only to ensure that the mechanism is correct and will generate exactly this kind of output on its own, with zero code changes, the When the scheduler has passed a number of real weeks, then moment is true. Do not treat any. In this folder make file a real historical report.
